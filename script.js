document.getElementById('contact-form').addEventListener('submit', function(e) {
    e.preventDefault();
    // Code to handle form submission (e.g., send data to backend or display a message)
    alert('Form submitted!');
  });
  